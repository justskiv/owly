# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and build
node_modules
dist
dist-ssr
*.local

# User data + command queue (commands/ lives inside data/)
data/

# Scratch / research notes that don't belong in history
tmp/

# Vitest browser-mode debug attachments (trace screenshots, etc.)
.vitest-attachments/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Internal-only material — not for the public repo:
# specs/design/working notes that target the author and
# collaborating agents, not someone reading the repo.
docs/
design/
spec/
CLAUDE.md

